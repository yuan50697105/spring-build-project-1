version: '3'
services:
  admin-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: admin-server
    container_name: admin-server
    hostname: admin-server
    ports:
    - 9080:8081
